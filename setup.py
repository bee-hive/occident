from setuptools import setup, find_packages

setup(
    name="occident",
    version="0.1",
    packages=find_packages(),
    description="A bioinformatics image processing pipeline package",
    author="Archit Verma (original code), Adam Weiner (refactor)",
    install_requires=[
        # Optionally, list additional dependencies if they are not already in your requirements.txt.
    ],
)
